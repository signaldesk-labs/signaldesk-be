# Module Architecture

This backend is organized around Flask modules for auth, dashboard, phone verification, and events.
Persistence uses MariaDB through Tortoise ORM with the asyncmy async driver.
